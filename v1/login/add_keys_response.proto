edition = "2023";

import "google/protobuf/duration.proto";

package UEventAnalysisToolAPI.V1.Login;

/**
 * Response to the user's request to add a public key.
 */
message AddKeyResponse {
   // A request identifier.
   string identifier = 1 [default = ""];
   // An additional message accompanying the gRPC return code.
   string details = 2;
}
