edition = "2023";

package UEventAnalysisToolAPI.V1.UserSettings;

/**
 * Response to user's request to revoke a key.  
 */
message RevokeKeyResponse {
   // The corresponding request identifier.
   string identifier = 1 [default = ""];
   // The details accompanying the gRPC response code.
   string details = 2;
}
